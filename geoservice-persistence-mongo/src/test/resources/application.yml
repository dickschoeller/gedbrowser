spring:
    profiles:
        active: test
    main:
        allow-bean-definition-overriding: true
logging:
    level:
        org:
            schoellerfamily: WARN
            springframework:
                test: WARN
                core: WARN
                beans: WARN
                data:
                    mongodb:
                        core:
                            index: OFF
