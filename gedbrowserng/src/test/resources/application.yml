gedbrowser:
    home: /var/lib/gedbrowser
    userFile: testUserFile.csv

server:
    servlet-path: /gedbrowserng

spring:
  profiles:
    active: test
  jackson:
    serialization:
      INDENT_OUTPUT: true
jwt:
  expires_in: 8
