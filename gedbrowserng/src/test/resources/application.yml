gedbrowser:
    home: src/test/resources
    userFile: ../gedbrowserng/src/test/resources/testUserFile.csv

server:
    servlet:
        context-path: /gedbrowserng

spring:
    profiles:
        active: test
    main:
        allow-bean-definition-overriding: true
    jackson:
        serialization:
            INDENT_OUTPUT: true
    data:
        mongodb:
            host: localhost
            port: 27017
jwt:
    expires_in: 600

geoservice:
    keyfile: stub
